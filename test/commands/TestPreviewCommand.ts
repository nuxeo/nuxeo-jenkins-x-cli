import {expect} from 'chai';
import {describe, it} from 'mocha';

describe('Preview Command', () => {
  it('does do the preview', () => {
    expect(true).to.be.eq(true, 'Expected something true');
  });
});
